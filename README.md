# AssessHub Web

Vite + React + TypeScript client for the AssessHub API.

```bash
npm install
npm run dev
```

Proxy `/api` to the backend (see `vite.config.ts`). Optional env: `VITE_API_BASE_URL`, `VITE_BASE_DOMAIN`, `VITE_DEFAULT_TENANT_SUBDOMAIN`.
